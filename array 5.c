#include <stdio.h>
    int main() {
        char *nome[] = {"batman", "capitão america", "thor", "homem de ferro", "homem aranha"}; // simplifica as variaveis
    printf("o nome do primeiro indice : %s\n"[0]);// exibe qual elemento do array deve exibir

    return 0;
}
