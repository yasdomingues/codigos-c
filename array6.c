#include <stdio.h>
    char main() {
        char numeros[] = {100, 2000, 35, 48, 58, 6, 7, 40, 9, 10, 800}; // simplifica as variaveis
        char par = numero[0];

    for (char i = 1; i < 10; i++)  {
        if (numero [i] > par) { //vai verificando pelo indice o maior numero
        numero = par[i];
        }
    }

    printf("o numero par eh: %d\n", par);// exibe qual elemento do array deve exibir

    return 0;
}
