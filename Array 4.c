#include <stdio.h>
    int main() {
        char *nome[4][10] = {"joao", "pedro", "maria", "ana"}; // simplifica as variaveis
    printf("o nome do terceiro indice: %s\n"[3]);// exibe qual elemento do array deve exibir

    return 0;
}
