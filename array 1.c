#include <stdio.h>
    int main() {
        int numero[4] = {1, 2, 3, 4, 5}; // simplifica as variaveis
    printf("elemento no indice 4: %d\n", numero[4]);// exibe qual elemento do array deve exibir

    return 0;
}
